@model IEnumerable<Doukala.Server.Core.Entities.Compagny>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>



<table class="table">


    @foreach (var item in Model)
    {
        <tr>
            @*<td>
                    @Html.DisplayFor(modelItem => item.LogoAvatar)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Logo)
                </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.Nom)
                @Html.DisplayFor(modelItem => item.Manager)
                @Html.DisplayFor(modelItem => item.SiretNumber)
                @Html.DisplayFor(modelItem => item.Description)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Activity)
                @Html.DisplayFor(modelItem => item.CodeNaf)
            </td>


            <td>
                @Html.DisplayFor(modelItem => item.IntraCommunityVat)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
                @Html.DisplayFor(modelItem => item.WebSite)
            </td>



            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
